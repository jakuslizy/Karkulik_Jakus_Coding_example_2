 - it should return nothing for an input type string
 - it should return nothing for an input of an empty array
 - it should return nothing for an input of an array size < 4
 - it should return nothing for an input of an array size > 6

 - it should return "6" for an input of [ 1, 2, 3, 4, 5] 
 - it should return "4" for an input of [ 2, 2, 3, 3 ] 
 - it should return "2" for an input of [ 6, 6, 4, 4, 1, 3 ] 
 - it should return "12" for an input of [ 3, 5, 3, 5, 4, 2 ]  
